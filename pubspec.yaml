name: cv
description: A new Flutter application.
publish_to: none
version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

  font_awesome_flutter: ^9.1.0
  url_launcher: any
  google_fonts: ^2.1.0
  http: ^0.13.3
  webview_flutter: ^3.0.2
  custom_rich_text: ^0.0.2
  lottie: ^1.3.0
  auto_size_text: ^3.0.0
  steps:
    git:
      url: https://github.com/lvchenyang/steps.git
      ref: master # commit hash

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
  assets:
    - image/
